# hdfx

Small Unix-style tools for working with HDF5.

## Install (local)

python3 -m venv .venv
source .venv/bin/activate
pip install -e .

Then add .venv/bin to your $PATH
You can get autocomplete with `hdfx --install-completion`

## Example

hdfx inspect data.h5
